{
  "name": "eslint-config-keep",
  "version": "0.1.1",
  "description": "ESLint shareable config for the Keep style",
  "author": "liamzebedee",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "node test/test.js",
    "lint": "eslint --config=./index.js *.js",
    "lint:fix": "eslint --config=./index.js --fix *.js"
  },
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "eslint": "^5.16.0"
  },
  "peerDependencies": {
    "eslint": ">=5.16.0"
  },
  "dependencies": {
    "eslint-config-google": "^0.13.0"
  }
}
